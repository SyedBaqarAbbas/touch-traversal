import { RoutePlaceholder } from "@/app/_components/route-placeholder";

export default function HomePage() {
  return (
    <RoutePlaceholder
      eyebrow="system overview"
      title="Explore the topologies of your thoughts."
      description="Touch Traversal will transform a folder of notes into a living knowledge graph, then let you move through it with a mouse, keyboard, or hand."
      status="repository foundation active"
    />
  );
}
